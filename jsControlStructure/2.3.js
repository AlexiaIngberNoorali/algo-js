
let x = 1;

while (1 <= 100) {
  if(x % 2 == 0){
    console.log(x);
  }
  x += 1; 
}



for (let x = 2; x <= 100; x+=2) {
 console.log(x);
}

//x+=2 == x=x+2
//EXECUTE ONCE ONLY.The first one is a command that will be executed only once, after the first execution of the loop.
//CONDITIONThe second one is a test that will be executed after each execution of the loop to know if we should continue or not (exactly like in while).
//WHAT IS EXECUTED (+ / * etc.)The third one is a command that will be executed after each execution of the loop.

  
